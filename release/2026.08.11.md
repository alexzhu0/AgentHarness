# 2026-08-11 AgentHarness Local Maintenance Closure

This record closes the AgentHarness repository's previously dirty evidence
contract milestone under explicit user authorization for local maintenance.
It preserves the historical T061 no-commit handoff and does not modify Pi.

## Highlights

- Adds the bounded observation/evidence v1 contract and CLI evaluator.
- Adds request/response schemas and canonicalization vectors.
- Closes absolute-path leakage in failure output.
- Closes labeled absolute-path leakage such as `path:/home/...` while
  preserving repository-relative paths and ordinary URLs.
- Rejects disclosure-bearing identifiers and cross-call response replay while
  documenting that same-request replay detection remains outside v1.
- Hardens snapshot capture against root replacement and bounds YAML structure.
- Validates Pi mapping argument digests, positional indices, and duplicate-free
  JSON fail closed.
- Preserves evidence-only, block-only, and `not_executed` boundaries.
- Documents the runtime-authorization `NO-GO` and continuity stop lines.
- Ignores generated Python bytecode and pytest caches.
- Adds repository guidance and a newest-first local release-record index.

## Verification

- Policy validation: passed.
- Smoke evaluation: 3/3 passed.
- Agent-bus and adapter-registry loop checks: passed.
- Changed-scope tests: 150/150 passed in 17.961s.
- Full AgentHarness suite: 389/389 passed in 34.527s.
- Wheel build: `agentharness-0.1.0-py3-none-any.whl`, 86,477 bytes; the archive
  includes the evidence-contract module and CLI entry point.
- Isolated wheel install and `agentharness --help` smoke test from outside the
  repository: passed.
- Python syntax, JSON parsing, local-link, line-length, and whitespace checks:
  passed.

## Release state

This is a source-tree and local-commit closure record. It is not a tag, pushed
release, public release page, package publication, deployment, runtime
authorization, or production certification.

The detailed scope and boundary record is
[`docs/29`](../docs/29-agentharness-maintenance-closure.md).
