# 2026-07-13 AgentHarness/Pi Shadow Milestone Handoff

T061 packages the accepted T056 evidence-integrity work, T057 evidence contract,
T058 block-only live wiring, T059 fake Win9-named live-shadow verification, and
the T060 runtime-authorization readiness ADR for review.

AgentHarness remains evidence-only. Pi or an external runtime owns
authorization. `allow_candidate` never permits execution; the current bridge is
block-only, all results are `not_executed`, and no real execution occurs.

T060 remains **NO-GO** and **NOT IMPLEMENTED**. This note is not permission to
power on, stage, commit, push, publish, or release.

See the explicit no-commit handoff and exact dirty manifests in
[`docs/26`](../docs/26-agentharness-pi-shadow-milestone-packaging.md).

## Final verification

AgentHarness policy validation passed; evaluation was 3/3; the `agent_bus` and
`adapter_registry` loop checks passed; targeted tests were 144/144 in 17.472s;
and the full suite was 381/381 in 34.265s. Diff checks passed. External Pi
import, path, and process findings were 0. Handoff counts were package 6,
manifest 4, mapping 20, and 7 evidence-response `result_status` values with 0
bad results. Instruction-file diff was none.

Pi's focused five AgentHarness files passed 211 tests with 0 skipped and 0
failed under `PI_AGENTHARNESS_E2E=1` using the real AgentHarness evaluator.
Cleanup removed 51 duplicate legacy helper tests after their unique
production-contract assertions were migrated. All tool fake sentinels and
execution counters stayed at 0. The focused synthetic `allow_candidate` block
probe passed, prepack passed, root `npm run check` passed over 668 files, and
diff checks passed.

The requested `npm --prefix packages/coding-agent pack` resolved the root
package under the current npm behavior, so its 1,272 files, 6.1 MB packed, and
18.2 MB unpacked are not target-package evidence. The correct command was:

```bash
cd packages/coding-agent && npm pack --dry-run --ignore-scripts
```

It verified `@earendil-works/pi-coding-agent@0.79.1`: 874 files, 5,053,559
bytes packed, and 13,435,171 bytes unpacked, with no tarball created. The target
package includes compiled `agentharness-contract-v1`,
`agentharness-dry-run-gate`, and `agent-session` JS, `.d.ts`, and map artifacts.
It has 0 stale fixture paths, 0 legacy runtime strings, and 0 `block:false`
branches.

The current tarball has 0 standalone AgentHarness schema JSON files. Schema IDs
and validators are compiled into contract artifacts. This is a packaging
observation for final review, not proof of standalone schema assets.

The initial `ai-slop-cleaner` review found four behavior-neutral smells. The
repair removed the duplicate test-only implementation after migrating its
unique assertions, removed dead fields, simplified the one-item verifier loop,
and renamed the runner. The post-repair cleaner result was **PASS**. Grounded,
fail-closed fallbacks were preserved, and generated caches were absent.
Excluded, pre-existing Pi `docs/others/` content remained checksum-unchanged
and is not T061 scope.

The final scoped documentation checks cover T061-added/changed hunks in
`README.md` and `docs/00-asset-map.md`, plus the milestone handoff and this
release note. Diff checks, local-link resolution, line lengths, and stale-term
searches passed. All scoped documentation remains at or below 80 characters
per line.

T061 documentation ownership includes the T061 hunks in `README.md` and
`docs/00-asset-map.md`, the milestone handoff, and this release note. The
NO-GO, NOT IMPLEMENTED, block-only, `not_executed`, no-real-execution,
no-commit, no-push, and no-release boundaries remain unchanged.
