# WSL Superpowers Workflow Migration

Date: 2026-08-13

## Status

Completed local development-workflow migration for the current WSL user and the
AgentHarness repository.

This is a source-tree maintenance record. It is not a Git tag, pushed release,
package publication, deployment, runtime authorization, or production
certification.

## Scope

- WSL Codex and the AgentHarness repository only.
- Windows Codex, Windows Claude, and WSL Claude were not changed.
- AgentHarness product behavior, evidence schemas, package version, and
  execution boundary were not changed.
- Historical OMX records remain truthful historical provenance.

The approved design and implementation plan are:

- [migration design](../docs/superpowers/specs/2026-08-13-omx-to-superpowers-migration-design.md)
- [implementation plan](../docs/superpowers/plans/2026-08-13-omx-to-superpowers-migration.md)

## Migration outcome

- Installed and enabled the official WSL Codex plugin
  `superpowers@openai-curated`, version `11c74d6b`.
- Verified the installed `using-superpowers`, `brainstorming`,
  `writing-plans`, `test-driven-development`,
  `requesting-code-review`, and `verification-before-completion` skills.
- Previewed OMX cleanup with `omx uninstall --dry-run --verbose`, then used
  `omx uninstall --verbose` to remove OMX-managed Codex hooks, configuration
  overlays, 29 skills, 37 prompts, 22 native agent configurations, and the
  global OMX `AGENTS.md`.
- The OMX cleanup removed Codex's Superpowers registration while preserving its
  plugin cache. Re-running the official `codex plugin add` command after OMX
  cleanup restored the final installed/enabled state.
- Moved remaining `~/.codex/.omx` and `~/.omx` state to the recoverable
  temporary directory
  `/tmp/agentharness-omx-migration-20260813/`.
- Removed global npm package `oh-my-codex@0.20.3`; npm removed 95 packages and
  `omx` is no longer on `PATH`.
- Moved AgentHarness `.omx/state`, `hud-config.json`, `setup-scope.json`,
  and `metrics.json` to
  `/tmp/agentharness-omx-migration-20260813/repository-runtime/`.
- Preserved ignored AgentHarness `.omx/plans`, `.omx/context`, and
  `.omx/logs` as historical evidence. No new work may use them as active
  workflow state.
- Preserved a user-owned Codex trust entry for the local
  `oh-my-codex` source checkout. A project trust entry is not an OMX hook,
  plugin, executable, package, skill, prompt, agent, or runtime state.

## GSD result

Exact-name checks found no WSL Codex skill/plugin path, global npm package,
`gsd` executable, AgentHarness tracked file, or matching Git commit for GSD or
`get-shit-done`. Therefore GSD removal was a verified no-op. Incidental
substring matches were not treated as deletion targets.

## Fresh verification

Run from the isolated AgentHarness migration worktree:

```text
PASS policy validation: examples/agent_policy.example.yaml
PASS PI-001
PASS PD-001
PASS SEC-001
Summary: 3/3 smoke evals passed
PASS loop bus validation: examples/agent_bus
PASS loop bus validation: examples/agent_bus_adapter_registry
Ran 389 tests in 36.115s
OK
PASS relative Markdown links: 64 Markdown files including this migration record
```

Environment completion probes:

```text
superpowers@openai-curated  installed, enabled  11c74d6b
command -v omx             absent
npm global oh-my-codex     absent
~/.codex/.omx              absent
~/.omx                     absent
command -v gsd             absent
```

Repository-local state probes confirmed that historical `.omx/plans`,
`.omx/context`, and `.omx/logs` remain in the original checkout, while the
active state, HUD, setup, and metrics targets are absent.
